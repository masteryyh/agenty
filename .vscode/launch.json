{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch agenty server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server.go",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}