{
  "name": "agenty-bootstrap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "build": "cargo build --release && bun run scripts/pack.ts",
    "test": "cargo test && bun test",
    "clean": "cargo clean && rm -rf dist"
  },
  "dependencies": {
    "@napi-rs/lzma": "1.5.1"
  },
  "devDependencies": {
    "agenty-cli": "workspace:*",
    "agenty-runtime": "workspace:*"
  }
}
